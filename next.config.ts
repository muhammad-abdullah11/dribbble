import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["cdn.dribbble.com", "images.unsplash.com", "res.cloudinary.com", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
